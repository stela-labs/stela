# @stela/cli

Command-line tools for operating Stela workflows, workers, migrations, and runs.

The `stela` binary expects `DATABASE_URL` and reads migrations from the current project's `migrations/` directory unless `--path` is provided.

See the repository README for installation, examples, and operational guidance:

https://github.com/stela-labs/stela
